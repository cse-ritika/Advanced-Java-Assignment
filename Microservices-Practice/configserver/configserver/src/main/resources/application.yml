server:
 port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sohail-12204238/spring-config-repo-2
          clone-on-start: true
